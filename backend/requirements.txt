fastapi
uvicorn[standard]
sqlalchemy
python-multipart
websockets
# Deployment engine: Kathara (multi-arch, Docker-SDK based, no sudo/host-netns).
kathara>=3.7.0,<4.0
# Docker SDK — Kathara builds on it; pinned here so it is explicit.
docker
