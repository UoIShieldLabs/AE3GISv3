# Alembic is driven programmatically (db.migrations.run_migrations) with the URL
# taken from Settings; this file only points at the migration scripts.
[alembic]
script_location = db/migrations
prepend_sys_path = .
